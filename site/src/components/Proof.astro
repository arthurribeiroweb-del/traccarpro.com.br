---
const items = [
  'Segurança e controle: saiba onde está seu veículo',
  'Economia de tempo: histórico e relatórios na palma da mão',
  'Aumenta as chances de localização por roubo',
];
---
<section class="border-b border-border/40 bg-surface/30 py-12 sm:py-20">
  <div class="mx-auto max-w-content px-4 sm:px-6">
    <h2 class="text-center text-2xl font-semibold tracking-tight text-white sm:text-3xl">
      Benefícios para você e sua família
    </h2>
    <ul class="mx-auto mt-12 max-w-2xl space-y-4">
      {items.map((item) => (
        <li class="flex min-w-0 gap-4 rounded-xl border border-border/60 bg-background/50 p-4 sm:p-5">
          <span class="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-accent/10 text-accent">
            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5" />
            </svg>
          </span>
          <span class="min-w-0 flex-1 text-[#e5e5e5] break-words">{item}</span>
        </li>
      ))}
    </ul>
  </div>
</section>
