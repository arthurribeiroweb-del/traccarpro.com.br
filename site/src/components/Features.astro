---
const features = [
  'Rastreamento em tempo real',
  'Histórico e replay de viagens',
  'Alertas (velocidade, cerca virtual, ignição, bateria desconectada)',
  'Bloqueio e desbloqueio pelo app',
  'Cerca virtual (entrada/saída)',
  'App para celular',
];
---
<section class="border-b border-border/40 py-12 sm:py-20">
  <div class="mx-auto max-w-content px-4 sm:px-6">
    <h2 class="text-center text-2xl font-semibold tracking-tight text-white sm:text-3xl">
      O que está incluso
    </h2>
    <ul class="mx-auto mt-12 grid gap-4 sm:grid-cols-2 lg:max-w-3xl lg:mx-auto">
      {
        features.map((f) => (
          <li class="flex min-w-0 gap-4 rounded-xl border border-border/60 bg-surface/30 p-4 sm:p-5">
            <span class="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-accent/10 text-accent" aria-hidden="true">
              <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="m4.5 12.75 6 6 9-13.5" />
              </svg>
            </span>
            <span class="min-w-0 flex-1 font-medium text-white break-words">{f}</span>
          </li>
        ))
      }
    </ul>
  </div>
</section>
